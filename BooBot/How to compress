By default, we use jscompress.com to shorten commands that are over 2k characters, this keeps the ~showjs readable.
However, some commands extend over. Those have an additional copy with minified code in the (you guessed it) "minified" folder.
